# Core dependencies for SLM-Math project

# PyTorch and deep learning
torch>=2.0.0
transformers>=4.30.0
accelerate>=0.20.0

# Dataset handling
datasets>=2.0.0
pandas>=2.0.0
huggingface_hub>=0.20.0  # For downloading models and datasets

# Utilities
tqdm>=4.65.0
pyyaml>=6.0

# Mathematical computation (for answer checking)
sympy>=1.12

# Parameter-Efficient Fine-Tuning (for training)
peft>=0.5.0

# Optional: Quantization (for QLoRA training)
bitsandbytes>=0.41.0  # Uncomment if using QLoRA

# Optional: OpenAI API (for CoT data generation)
openai>=1.0.0  # Uncomment if using build_CoT_data.py

# Optional: vLLM for accelerated inference (requires CUDA 11.8+)
# Install separately: pip install vllm
vllm>=0.4.0

# Security: Maintained replacement for pycrypto
pycryptodome>=3.18.0
